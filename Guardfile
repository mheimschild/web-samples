# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{views/.+\.(erb|jade|slim)$})
  watch(%r{public/javascripts/.+\.js$})
  watch(%r{public/stylesheets/.+\.less$})
  watch(%r{routes/.+\.js$})
  # Rails Assets Pipeline
end
